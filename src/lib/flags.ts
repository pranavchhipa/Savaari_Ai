// Feature flags — compile-time constants only.
// Flip ENABLE_PERSONAS to false to fall back to the pre-v2 flow
// (no PersonaPicker, no RefinementBar, legacy single-stage AI).
export const ENABLE_PERSONAS = true;
